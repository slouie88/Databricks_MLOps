# This is a Databricks asset bundle definition for Databricks_MLOps.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: Databricks_MLOps

targets:
  dev:
    default: true
    mode: development
    workspace:
      host: https://dbc-95e5cd53-4acb.cloud.databricks.com/
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.target}/${bundle.name}
      profile: databricks_mlops
    variables:
      schedule_pause_status: PAUSED

  test:
    presets:
      name_prefix: 'test_'
    workspace:
      host: https://dbc-95e5cd53-4acb.cloud.databricks.com/
      root_path: /Shared/.bundle/${bundle.target}/${bundle.name}
      profile: databricks_mlops
    variables:
      schedule_pause_status: PAUSED

  prod:
    mode: production
    workspace:
      host: https://dbc-95e5cd53-4acb.cloud.databricks.com/
      root_path: /Shared/.bundle/${bundle.target}/${bundle.name}
      profile: databricks_mlops
    variables:
      schedule_pause_status: PAUSED # normally UNPAUSED